export const initialAppState = {
  loading: false,
  loaded: false,
  data: null,
};

export const appReducer = (state, action) => {

  switch (action.type) {

    default:
      return state;
  }
};
